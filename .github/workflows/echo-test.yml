name: Explore Ubuntu Environment

on: [push]

jobs:
  build-and-run:
    runs-on: ubuntu-slim
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Compile C Program
        run: gcc hello.c -o my_git_app

      - name: Execute Program
        run: ./my_git_app
