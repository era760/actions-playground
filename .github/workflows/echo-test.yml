name: Ubuntu Echo Test

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Benchmark Xeon Power
        run: |
          echo "=== 円周率5000桁の計算にかかる時間を計測！ ==="
          time echo "scale=5000; 4*a(1)" | bc -l
