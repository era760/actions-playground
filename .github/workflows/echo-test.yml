name: Ubuntu Echo Test

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Check CPU and Memory
        run: |
          echo "=== CPU情報 ==="
          lscpu | grep "Model name"
          echo "=== メモリ情報 ==="
          free -h
